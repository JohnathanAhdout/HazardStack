# SPIRAL Web App Environment Variables

# API Backend URL
# For local development with API running on your machine:
NEXT_PUBLIC_API_URL=http://localhost:8000

# For production deployment:
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# Note: The web app works with mock data if the API is not available
# You can start using it immediately without setting up the backend
